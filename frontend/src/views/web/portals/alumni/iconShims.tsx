/**
 * Alumni iconShims — re-export Student Portal's MCI shim.
 * Both portals share identical glyph semantics; only the colour hue
 * differs (purple → orange) at the consumer-style level.
 */
export * from '../student/iconShims';
