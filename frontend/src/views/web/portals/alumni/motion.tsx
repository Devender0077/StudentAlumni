/**
 * Alumni motion — re-export Student Portal's motion primitives.
 * (Animation APIs are theme-agnostic; gradient colours come from
 * alumni/gradients.ts when needed.)
 */
export * from '../student/motion';
