/**
 * /me — Standalone SA Profile Web page.
 * Reuses the Profile Web Shell from /(tabs)/profile but outside the tab bar,
 * so clicking "Profile" from the Student Dashboard sidebar opens a full-screen
 * standalone profile editor (matches the "SA Profile Web Standalone" spec).
 */
export { default } from './(tabs)/profile';
